name: Dashboard Testing and Formatting

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - master
      - improvements
      - 'releases/**'
  pull_request:
    branches:
      - master
      - improvements
      - 'releases/**'
  merge_group:

jobs:
  dashboard:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Install dependencies and check
      run: |
        npm --prefix sky/dashboard install
        npm --prefix sky/dashboard test -- --runInBand src/components/services.test.jsx src/data/connectors/services.test.jsx src/tests/service-details.test.jsx
        npm --prefix sky/dashboard run lint
        npm --prefix sky/dashboard run format:check
        npm --prefix sky/dashboard run build
