# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc
import warnings

from sky.schemas.generated import healthv1_pb2 as sky_dot_schemas_dot_generated_dot_healthv1__pb2

GRPC_GENERATED_VERSION = '1.63.0'
GRPC_VERSION = grpc.__version__
EXPECTED_ERROR_RELEASE = '1.65.0'
SCHEDULED_RELEASE_DATE = 'June 25, 2024'
_version_not_supported = False

try:
    from grpc._utilities import first_version_is_lower
    _version_not_supported = first_version_is_lower(GRPC_VERSION, GRPC_GENERATED_VERSION)
except ImportError:
    _version_not_supported = True

if _version_not_supported:
    warnings.warn(
        f'The grpc package installed is at version {GRPC_VERSION},'
        + f' but the generated code in sky/schemas/generated/healthv1_pb2_grpc.py depends on'
        + f' grpcio>={GRPC_GENERATED_VERSION}.'
        + f' Please upgrade your grpc module to grpcio>={GRPC_GENERATED_VERSION}'
        + f' or downgrade your generated code using grpcio-tools<={GRPC_VERSION}.'
        + f' This warning will become an error in {EXPECTED_ERROR_RELEASE},'
        + f' scheduled for release on {SCHEDULED_RELEASE_DATE}.',
        RuntimeWarning
    )


class HealthServiceStub(object):
    """Cluster runtime health, served by skylet on the head node.

    Rationale: the cluster health probe historically SSH-exec'd `ray status`
    from the API server and parsed its text output. The SSH transport adds a
    whole class of false positives (transient transport failures flagging a
    healthy cluster) and is expensive at scale. Skylet is colocated with the
    ray head, so it can run the same check locally and return the result over
    the existing skylet gRPC channel.

    GetRayStatus deliberately returns the RAW `ray status` result
    (returncode/stdout/stderr) rather than parsed counts: the caller keeps
    using the exact same parser as the legacy SSH path, so the two transports
    cannot drift semantically. Future RPCs on this service can add structured
    per-node state (e.g. from ray.nodes()) for consumers that need node
    identity.
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.GetRayStatus = channel.unary_unary(
                '/health.v1.HealthService/GetRayStatus',
                request_serializer=sky_dot_schemas_dot_generated_dot_healthv1__pb2.GetRayStatusRequest.SerializeToString,
                response_deserializer=sky_dot_schemas_dot_generated_dot_healthv1__pb2.GetRayStatusResponse.FromString,
                _registered_method=True)


class HealthServiceServicer(object):
    """Cluster runtime health, served by skylet on the head node.

    Rationale: the cluster health probe historically SSH-exec'd `ray status`
    from the API server and parsed its text output. The SSH transport adds a
    whole class of false positives (transient transport failures flagging a
    healthy cluster) and is expensive at scale. Skylet is colocated with the
    ray head, so it can run the same check locally and return the result over
    the existing skylet gRPC channel.

    GetRayStatus deliberately returns the RAW `ray status` result
    (returncode/stdout/stderr) rather than parsed counts: the caller keeps
    using the exact same parser as the legacy SSH path, so the two transports
    cannot drift semantically. Future RPCs on this service can add structured
    per-node state (e.g. from ray.nodes()) for consumers that need node
    identity.
    """

    def GetRayStatus(self, request, context):
        """Run `ray status` locally on the head node against the ray port
        SkyPilot launched, and return its raw result.
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_HealthServiceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'GetRayStatus': grpc.unary_unary_rpc_method_handler(
                    servicer.GetRayStatus,
                    request_deserializer=sky_dot_schemas_dot_generated_dot_healthv1__pb2.GetRayStatusRequest.FromString,
                    response_serializer=sky_dot_schemas_dot_generated_dot_healthv1__pb2.GetRayStatusResponse.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'health.v1.HealthService', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))


 # This class is part of an EXPERIMENTAL API.
class HealthService(object):
    """Cluster runtime health, served by skylet on the head node.

    Rationale: the cluster health probe historically SSH-exec'd `ray status`
    from the API server and parsed its text output. The SSH transport adds a
    whole class of false positives (transient transport failures flagging a
    healthy cluster) and is expensive at scale. Skylet is colocated with the
    ray head, so it can run the same check locally and return the result over
    the existing skylet gRPC channel.

    GetRayStatus deliberately returns the RAW `ray status` result
    (returncode/stdout/stderr) rather than parsed counts: the caller keeps
    using the exact same parser as the legacy SSH path, so the two transports
    cannot drift semantically. Future RPCs on this service can add structured
    per-node state (e.g. from ray.nodes()) for consumers that need node
    identity.
    """

    @staticmethod
    def GetRayStatus(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/health.v1.HealthService/GetRayStatus',
            sky_dot_schemas_dot_generated_dot_healthv1__pb2.GetRayStatusRequest.SerializeToString,
            sky_dot_schemas_dot_generated_dot_healthv1__pb2.GetRayStatusResponse.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
