{{- if .Values.apiService.highAvailability.enabled }}
{{- $fullName := include "skypilot.fullname" . -}}
{{- $haConfig := .Values.apiService.highAvailability | default (dict) -}}
{{- $readinessDrainSeconds := dig "readinessDrainSeconds" 20 $haConfig -}}
{{- $controllerService := .Values.controllerService | default (dict) -}}
{{- $executorService := .Values.executorService | default (dict) -}}
{{- $controllerImage := default .Values.apiService.image (get $controllerService "image") -}}
{{- $controllerPullPolicy := default .Values.apiService.imagePullPolicy (get $controllerService "imagePullPolicy") -}}
{{- $controllerReplicas := dig "replicas" 2 $controllerService -}}
{{- $controllerHealthPort := dig "healthPort" 46582 $controllerService -}}
{{- if and .Values.apiService.metrics.enabled (eq (int $controllerHealthPort) (int .Values.apiService.metrics.port)) -}}
{{- fail "controllerService.healthPort must differ from apiService.metrics.port when metrics are enabled" -}}
{{- end -}}
{{- $controllerTerminationGracePeriodSeconds := dig "terminationGracePeriodSeconds" 60 $controllerService -}}
{{- $executorTerminationGracePeriodSeconds := dig "terminationGracePeriodSeconds" 60 $executorService -}}
{{- $controllerCutoverQuiescenceSeconds := add $executorTerminationGracePeriodSeconds 10 -}}
{{- $controllerResources := dig "resources" .Values.apiService.resources $controllerService -}}
{{- $controllerExtraEnvs := get $controllerService "extraEnvs" -}}
{{- $controllerExtraVolumes := get $controllerService "extraVolumes" -}}
{{- $controllerExtraVolumeMounts := get $controllerService "extraVolumeMounts" -}}
{{- $controllerNodeSelector := get $controllerService "nodeSelector" -}}
{{- $controllerTolerations := get $controllerService "tolerations" -}}
{{- $controllerAffinity := get $controllerService "affinity" -}}
{{- $controllerTopologySpreadTopologyKey := "kubernetes.io/hostname" -}}
{{- if hasKey $controllerService "topologySpreadTopologyKey" -}}
{{- $controllerTopologySpreadTopologyKey = get $controllerService "topologySpreadTopologyKey" -}}
{{- end -}}
{{- $externalLB := .Values.serve.externalLoadBalancer | default (dict) -}}
{{- $externalLBEnabled := get $externalLB "enabled" | default false -}}
{{- $externalLBResources := get $externalLB "resources" | default (dict) -}}
{{- $externalLBAuth := get $externalLB "auth" | default (dict) -}}
{{- $lbSyncAuth := get $externalLBAuth "lbSync" | default (dict) -}}
{{- $controllerAdminAuth := get $externalLBAuth "controllerAdmin" | default (dict) -}}
{{- $lbDataPlaneAuth := get $externalLBAuth "lbDataPlane" | default (dict) -}}
{{- $lbSyncSecret := get $lbSyncAuth "existingSecret" | default "" -}}
{{- $lbSyncKey := get $lbSyncAuth "key" | default "" -}}
{{- $controllerAdminSecret := get $controllerAdminAuth "existingSecret" | default "" -}}
{{- $controllerAdminKey := get $controllerAdminAuth "key" | default "" -}}
{{- $lbDataPlaneSecret := get $lbDataPlaneAuth "existingSecret" | default "" -}}
{{- $lbDataPlaneKey := get $lbDataPlaneAuth "key" | default "" -}}
{{- $lbDataPlaneAuthEnabled := and $externalLBEnabled (not (empty $lbDataPlaneSecret)) (not (empty $lbDataPlaneKey)) -}}
{{- $resourceActions := .Values.resourceActions | default (dict) -}}
{{- $authorityWorker := get $resourceActions "authorityWorker" | default (dict) -}}
{{- $authorityWorkerEnabled := get $authorityWorker "enabled" | default false -}}
{{- $authorityActiveCohort := get $authorityWorker "activeCohort" | default "" -}}
{{- $authorityCohorts := get $authorityWorker "cohorts" | default (list) -}}
{{- $authorityPreflightEnabled := and $authorityWorkerEnabled (gt (len $authorityCohorts) 0) -}}
{{- $authorityAuth := get $authorityWorker "auth" | default (dict) -}}
{{- $authorityTLS := get $authorityWorker "tls" | default (dict) -}}
{{- $authorityAuthSecret := get $authorityAuth "existingSecret" | default "" -}}
{{- $authorityAuthKey := get $authorityAuth "key" | default "" -}}
{{- $authorityTLSSecret := get $authorityTLS "existingSecret" | default "" -}}
{{- $authorityTLSCAKey := get $authorityTLS "caKey" | default "" -}}
{{- $authorityManifestConfigMap := printf "%s-authority-%s-manifest" $fullName $authorityActiveCohort -}}
{{- /* The activation marker has no user-owned compatibility meaning. Keep it
      reserved even when this role has no live authority preflight wiring. */ -}}
{{- $controllerReservedEnvNames := list "KUBECONFIG" "POD_IP" "PROMETHEUS_MULTIPROC_DIR" "SKY_API_SERVER_METRICS_ENABLED" "SKYPILOT_API_DEPLOYMENT_NAME" "SKYPILOT_API_REQUEST_BACKEND" "SKYPILOT_API_REQUEST_CUTOVER_GATE_PATH" "SKYPILOT_API_SERVER_ENDPOINT" "SKYPILOT_API_SERVER_INSTANCE_ID" "SKYPILOT_API_SERVER_ROLE" "SKYPILOT_API_SERVER_STORAGE_ENABLED" "SKYPILOT_CONTROLLER_CUTOVER_QUIESCENCE_SECONDS" "SKYPILOT_DB_CONNECTION_URI" "SKYPILOT_DEV" "SKYPILOT_GRACE_PERIOD_SECONDS" "SKYPILOT_IN_CLUSTER_NAMESPACE" "SKYPILOT_POD_CPU_CORE_LIMIT" "SKYPILOT_POD_MEMORY_BYTES_LIMIT" "SKYPILOT_POD_NAME" "SKYPILOT_POD_NAMESPACE" "SKYPILOT_POD_UID" "SKYPILOT_RELEASE_NAME" "SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED" "SKYPILOT_ROLLING_UPDATE_ENABLED" "SKYPILOT_SERVE_API_SERVICE_URL" "SKYPILOT_SERVE_CONTROLLER_ADMIN_AUTH_TOKENS_FILE" "SKYPILOT_SERVE_EXTERNAL_LB_ENABLED" "SKYPILOT_SERVE_LB_AUTH_TOKENS_FILE" "SKYPILOT_SERVE_LB_DATA_PLANE_AUTH_ENABLED" "SKYPILOT_SERVE_LB_HA_RBAC_READY" "SKYPILOT_SERVE_LB_RESOURCES_JSON" "SKYPILOT_SERVE_LB_SYNC_AUTH_TOKENS_FILE" "SKYPILOT_STATE_DB_MIGRATION_MODE" -}}
{{- if $authorityPreflightEnabled -}}
{{- $controllerReservedEnvNames = append $controllerReservedEnvNames "SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE" -}}
{{- end -}}
{{- if .Values.awsCredentials.enabled -}}
{{- $controllerReservedEnvNames = concat $controllerReservedEnvNames (list "AWS_ACCESS_KEY_ID" "AWS_SECRET_ACCESS_KEY") -}}
{{- end -}}
{{- if .Values.digitaloceanCredentials.enabled -}}
{{- $controllerReservedEnvNames = append $controllerReservedEnvNames "DIGITALOCEAN_CREDENTIALS" -}}
{{- end -}}
{{- if .Values.gcpCredentials.enabled -}}
{{- $controllerReservedEnvNames = append $controllerReservedEnvNames "GOOGLE_APPLICATION_CREDENTIALS" -}}
{{- end -}}
{{- if .Values.lambdaCredentials.enabled -}}
{{- $controllerReservedEnvNames = append $controllerReservedEnvNames "LAMBDA_API_KEY" -}}
{{- end -}}
{{- if .Values.r2Credentials.enabled -}}
{{- $controllerReservedEnvNames = concat $controllerReservedEnvNames (list "R2_ACCOUNT_ID" "R2_CREDENTIALS") -}}
{{- end -}}
{{- if .Values.runpodCredentials.enabled -}}
{{- $controllerReservedEnvNames = append $controllerReservedEnvNames "RUNPOD_API_KEY" -}}
{{- end -}}
{{- if .Values.vastCredentials.enabled -}}
{{- $controllerReservedEnvNames = append $controllerReservedEnvNames "VAST_API_KEY" -}}
{{- end -}}

{{- $controllerReservedVolumeNames := list "skypilot-config" "skypilot-role-runtime" "skypilot-serve-controller-admin-auth" "skypilot-serve-lb-auth" "skypilot-serve-lb-sync-auth" "skypilot-server-config" "skypilot-ssh-identity" "skypilot-ssh-node-pools" "state-volume" -}}
{{- if $authorityPreflightEnabled -}}
{{- $controllerReservedVolumeNames = concat $controllerReservedVolumeNames (list "skypilot-resource-action-authority-auth" "skypilot-resource-action-authority-ca" "skypilot-resource-action-authority-manifest") -}}
{{- end -}}
{{- if .Values.awsCredentials.enabled -}}
{{- $controllerReservedVolumeNames = concat $controllerReservedVolumeNames (list "aws-config" "aws-config-files") -}}
{{- end -}}
{{- if .Values.coreweaveCredentials.enabled -}}
{{- $controllerReservedVolumeNames = concat $controllerReservedVolumeNames (list "coreweave-config" "coreweave-credentials") -}}
{{- end -}}
{{- if .Values.digitaloceanCredentials.enabled -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "digitalocean-config" -}}
{{- end -}}
{{- if .Values.gcpCredentials.enabled -}}
{{- $controllerReservedVolumeNames = concat $controllerReservedVolumeNames (list "gcp-config" "gcp-credentials") -}}
{{- end -}}
{{- if .Values.kubernetesCredentials.useKubeconfig -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "kube-config" -}}
{{- end -}}
{{- if .Values.lambdaCredentials.enabled -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "lambda-config" -}}
{{- end -}}
{{- if .Values.nebiusCredentials.enabled -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "nebius-credentials" -}}
{{- end -}}
{{- if .Values.r2Credentials.enabled -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "r2-config" -}}
{{- end -}}
{{- if .Values.runpodCredentials.enabled -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "runpod-config" -}}
{{- end -}}
{{- if .Values.slurmCredentials.config -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "slurm-config" -}}
{{- end -}}
{{- if .Values.vastCredentials.enabled -}}
{{- $controllerReservedVolumeNames = append $controllerReservedVolumeNames "vast-config" -}}
{{- end -}}

{{- $controllerReservedMountPaths := list "/etc/skypilot/serve-auth/controller-admin" "/etc/skypilot/serve-auth/lb-data-plane" "/etc/skypilot/serve-auth/lb-sync" "/root/.sky" "/root/.sky/.server.yaml" "/root/.ssh" "/root/sky_logs" "/var/run/skypilot" "/var/skypilot/config" "/var/skypilot/ssh_keys" "/var/skypilot/ssh_node_pool" -}}
{{- if $authorityPreflightEnabled -}}
{{- $controllerReservedMountPaths = concat $controllerReservedMountPaths (list "/etc/skypilot/resource-action-authority/auth" "/etc/skypilot/resource-action-authority/manifest.json" "/etc/skypilot/resource-action-authority/tls") -}}
{{- end -}}
{{- if .Values.awsCredentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.aws" -}}
{{- end -}}
{{- if .Values.coreweaveCredentials.enabled -}}
{{- $controllerReservedMountPaths = concat $controllerReservedMountPaths (list "/root/.coreweave" "/root/.coreweave_credentials") -}}
{{- end -}}
{{- if .Values.digitaloceanCredentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.config/doctl" -}}
{{- end -}}
{{- if .Values.gcpCredentials.enabled -}}
{{- $controllerReservedMountPaths = concat $controllerReservedMountPaths (list "/root/.config/gcloud" "/root/gcp-cred.json") -}}
{{- end -}}
{{- if .Values.kubernetesCredentials.useKubeconfig -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/var/skypilot/kubeconfig" -}}
{{- end -}}
{{- if .Values.apiService.sshNodePools -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.kube" -}}
{{- else if .Values.kubernetesCredentials.useKubeconfig -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.kube" -}}
{{- end -}}
{{- if .Values.lambdaCredentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.lambda_cloud" -}}
{{- end -}}
{{- if .Values.nebiusCredentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.nebius_credentials" -}}
{{- end -}}
{{- if .Values.r2Credentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.cloudflare" -}}
{{- end -}}
{{- if .Values.runpodCredentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.runpod" -}}
{{- end -}}
{{- if .Values.slurmCredentials.config -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.slurm" -}}
{{- end -}}
{{- if .Values.vastCredentials.enabled -}}
{{- $controllerReservedMountPaths = append $controllerReservedMountPaths "/root/.config/vastai" -}}
{{- end -}}
{{ include "skypilot.validatePodExtras" (dict "envLists" (list .Values.global.extraEnvs .Values.databaseConnection.extraEnvs .Values.apiService.extraEnvs $controllerExtraEnvs) "reservedEnvNames" $controllerReservedEnvNames "volumeLists" (list .Values.databaseConnection.extraVolumes .Values.apiService.extraVolumes $controllerExtraVolumes) "reservedVolumeNames" $controllerReservedVolumeNames "mountLists" (list .Values.databaseConnection.extraVolumeMounts .Values.apiService.extraVolumeMounts $controllerExtraVolumeMounts) "reservedMountPaths" $controllerReservedMountPaths) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $fullName }}-controller
  namespace: {{ .Release.Namespace }}
spec:
  replicas: {{ $controllerReplicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: {{ $fullName }}-controller
  template:
    metadata:
      annotations:
        {{- if .Values.apiService.metrics.enabled }}
        {{- if .Values.prometheus.useDedicatedScrapeConfig }}
        skypilot.co/scrape: "true"
        skypilot.co/path: "/metrics"
        skypilot.co/port: {{ .Values.apiService.metrics.port | quote }}
        {{- else }}
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: {{ .Values.apiService.metrics.port | quote }}
        {{- end }}
        {{- end }}
      labels:
        app: {{ $fullName }}-controller
        skypilot.co/role: controller
    spec:
      automountServiceAccountToken: {{ or .Values.kubernetesCredentials.useApiServerCluster $externalLBEnabled }}
      serviceAccountName: {{ include "skypilot.serviceAccountName" . }}
      terminationGracePeriodSeconds: {{ $controllerTerminationGracePeriodSeconds }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      runtimeClassName: {{ .Values.runtimeClassName }}
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: skypilot-controller
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        imagePullPolicy: {{ $controllerPullPolicy }}
        {{- with .Values.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml $controllerResources | nindent 10 }}
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/sh
              - -c
              - touch /var/run/skypilot/draining && sleep {{ $readinessDrainSeconds }}
        env:
        {{- with .Values.global.extraEnvs }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.databaseConnection.extraEnvs }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.apiService.extraEnvs }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with $controllerExtraEnvs }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        - name: SKYPILOT_DEV
          value: {{ .Values.apiService.skypilotDev | quote }}
        - name: SKYPILOT_RELEASE_NAME
          value: {{ $fullName | quote }}
        - name: SKYPILOT_API_REQUEST_BACKEND
          value: postgres
        - name: SKYPILOT_API_REQUEST_CUTOVER_GATE_PATH
          value: {{ .Values.requestStore.cutoverGatePath | quote }}
        - name: SKYPILOT_API_SERVER_ENDPOINT
          value: {{ printf "http://%s-api-service.%s.svc" $fullName .Release.Namespace | quote }}
        - name: SKYPILOT_API_SERVER_ROLE
          value: controller
        {{- if .Values.apiService.metrics.enabled }}
        - name: SKY_API_SERVER_METRICS_ENABLED
          value: "true"
        - name: PROMETHEUS_MULTIPROC_DIR
          value: /tmp/metrics
        {{- end }}
        - name: SKYPILOT_CONTROLLER_CUTOVER_QUIESCENCE_SECONDS
          value: {{ $controllerCutoverQuiescenceSeconds | quote }}
        - name: SKYPILOT_API_SERVER_INSTANCE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.uid
        - name: SKYPILOT_POD_UID
          valueFrom:
            fieldRef:
              fieldPath: metadata.uid
        - name: SKYPILOT_POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: SKYPILOT_POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: SKYPILOT_ROLLING_UPDATE_ENABLED
          value: "true"
        - name: SKYPILOT_API_SERVER_STORAGE_ENABLED
          value: "true"
        - name: SKYPILOT_GRACE_PERIOD_SECONDS
          value: {{ $controllerTerminationGracePeriodSeconds | quote }}
        {{- if .Values.apiService.dbConnectionSecretName }}
        - name: SKYPILOT_DB_CONNECTION_URI
          valueFrom:
            secretKeyRef:
              name: {{ .Values.apiService.dbConnectionSecretName }}
              key: connection_string
        {{- else }}
        - name: SKYPILOT_DB_CONNECTION_URI
          valueFrom:
            secretKeyRef:
              name: {{ $fullName }}-db-connection
              key: connection_string
        {{- end }}
        - name: SKYPILOT_STATE_DB_MIGRATION_MODE
          value: verify
        - name: SKYPILOT_POD_CPU_CORE_LIMIT
          valueFrom:
            resourceFieldRef:
              containerName: skypilot-controller
              resource: requests.cpu
        - name: SKYPILOT_POD_MEMORY_BYTES_LIMIT
          valueFrom:
            resourceFieldRef:
              containerName: skypilot-controller
              resource: requests.memory
        {{- if .Values.gcpCredentials.enabled }}
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: /root/gcp-cred.json
        {{- end }}
        {{- if .Values.kubernetesCredentials.inclusterNamespace }}
        - name: SKYPILOT_IN_CLUSTER_NAMESPACE
          value: {{ .Values.kubernetesCredentials.inclusterNamespace }}
        {{- end }}
        {{- if and .Values.kubernetesCredentials.useKubeconfig .Values.apiService.sshNodePools }}
        - name: KUBECONFIG
          value: /root/.kube/config:/var/skypilot/kubeconfig/config
        {{- end }}
        - name: SKYPILOT_SERVE_EXTERNAL_LB_ENABLED
          value: {{ ternary "true" "false" $externalLBEnabled | quote }}
        {{- if $authorityPreflightEnabled }}
        - name: SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED
          value: "true"
        - name: SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE
          value: /etc/skypilot/resource-action-authority/auth/tokens
        {{- end }}
        {{- if $externalLBEnabled }}
        - name: SKYPILOT_SERVE_LB_HA_RBAC_READY
          value: "true"
        - name: SKYPILOT_API_DEPLOYMENT_NAME
          value: {{ printf "%s-api-server" $fullName | quote }}
        - name: SKYPILOT_SERVE_API_SERVICE_URL
          value: {{ printf "http://%s-api-service.%s.svc" $fullName .Release.Namespace | quote }}
        - name: SKYPILOT_SERVE_LB_RESOURCES_JSON
          value: {{ toJson $externalLBResources | quote }}
        - name: SKYPILOT_SERVE_LB_SYNC_AUTH_TOKENS_FILE
          value: /etc/skypilot/serve-auth/lb-sync/tokens
        - name: SKYPILOT_SERVE_CONTROLLER_ADMIN_AUTH_TOKENS_FILE
          value: /etc/skypilot/serve-auth/controller-admin/tokens
        - name: SKYPILOT_SERVE_LB_DATA_PLANE_AUTH_ENABLED
          value: {{ ternary "true" "false" $lbDataPlaneAuthEnabled | quote }}
        {{- if $lbDataPlaneAuthEnabled }}
        - name: SKYPILOT_SERVE_LB_AUTH_TOKENS_FILE
          value: /etc/skypilot/serve-auth/lb-data-plane/tokens
        {{- end }}
        {{- end }}
        command: ["tini", "--"]
        args:
        - /bin/sh
        - -c
        - |
          set -e
          {{- if .Values.apiService.metrics.enabled }}
          # Make the directory available to pre-deploy hooks. It is reset
          # after every hook/setup command and immediately before Python.
          mkdir -p /tmp/metrics
          {{- end }}
          {{- if .Values.apiService.preDeployHook }}
          {{ .Values.apiService.preDeployHook | nindent 10 }}
          {{- end }}
          mkdir -p /root/.sky
          if [ -L /root/.sky/config.yaml ]; then
            rm -f /root/.sky/config.yaml
          fi
          if [ ! -s /root/.sky/config.yaml ]; then
            config_tmp="/root/.sky/.config.yaml.${SKYPILOT_POD_UID}.tmp"
            cp /var/skypilot/config/config.yaml "$config_tmp"
            if [ ! -e /root/.sky/config.yaml ]; then
              mv -n "$config_tmp" /root/.sky/config.yaml
            fi
            if [ ! -s /root/.sky/config.yaml ]; then
              mv -f "$config_tmp" /root/.sky/config.yaml
            else
              rm -f "$config_tmp"
            fi
          fi
          {{- if .Values.apiService.sshNodePools }}
          ln -sf /var/skypilot/ssh_node_pool/ssh_node_pools.yaml /root/.sky/ssh_node_pools.yaml
          mkdir -p /root/.kube
          if [ ! -s /root/.kube/config ]; then
            kubeconfig_tmp="/root/.kube/.config.${SKYPILOT_POD_UID}.tmp"
            printf '{}\n' > "$kubeconfig_tmp"
            if [ ! -e /root/.kube/config ]; then
              mv -n "$kubeconfig_tmp" /root/.kube/config
            fi
            if [ ! -s /root/.kube/config ]; then
              mv -f "$kubeconfig_tmp" /root/.kube/config
            else
              rm -f "$kubeconfig_tmp"
            fi
          fi
          {{- end }}
          {{- if .Values.apiService.sshKeySecret }}
          mkdir -p /root/.ssh
          for file in /var/skypilot/ssh_keys/*; do
            if [ -f "$file" ]; then
              ln -sf "$file" "/root/.ssh/$(basename "$file")"
            fi
          done
          {{- end }}
          {{- if .Values.nebiusCredentials.enabled }}
          mkdir -p /root/.nebius
          for file in /root/.nebius_credentials/*; do
            if [ -f "$file" ]; then
              ln -sf "$file" "/root/.nebius/$(basename "$file")"
            fi
          done
          echo "{{ .Values.nebiusCredentials.tenantId }}" > /root/.nebius/NEBIUS_TENANT_ID.txt
          nebius profile create --profile sky --endpoint api.nebius.cloud --service-account-file /root/.nebius/credentials.json || true
          {{- end }}
          {{- if .Values.apiService.metrics.enabled }}
          # The reaper removes only dead-process live gauges. Counters and
          # histograms survive, so never inherit files created by a hook or a
          # previous supervisor invocation.
          export PROMETHEUS_MULTIPROC_DIR=/tmp/metrics
          rm -rf /tmp/metrics
          mkdir -p /tmp/metrics
          {{- end }}
          exec python -m sky.server.server --deploy --host 0.0.0.0 \
            --role controller --role-health-port {{ $controllerHealthPort }} \
            --metrics-port {{ .Values.apiService.metrics.port }}
        ports:
        - name: health
          containerPort: {{ $controllerHealthPort }}
        {{- if .Values.apiService.metrics.enabled }}
        - name: metrics
          containerPort: {{ .Values.apiService.metrics.port }}
        {{- end }}
        startupProbe:
          httpGet:
            path: /readyz
            port: health
          failureThreshold: 60
          periodSeconds: 10
          timeoutSeconds: 1
        livenessProbe:
          httpGet:
            path: /livez
            port: health
          periodSeconds: 10
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            path: /readyz
            port: health
          failureThreshold: 3
          periodSeconds: 5
          timeoutSeconds: 1
        volumeMounts:
        - name: skypilot-role-runtime
          mountPath: /var/run/skypilot
        - name: state-volume
          mountPath: /root/.sky
          subPath: .sky
        - name: state-volume
          mountPath: /root/.ssh
          subPath: .ssh
        - name: state-volume
          mountPath: /root/sky_logs
          subPath: sky_logs
        - name: skypilot-config
          mountPath: /var/skypilot/config
        {{- if or .Values.auth.externalProxy.enabled (eq (include "skypilot.ingressOAuthEnabled" .) "true") }}
        - name: skypilot-server-config
          mountPath: /root/.sky/.server.yaml
          subPath: server.yaml
          readOnly: true
        {{- end }}
        {{- if .Values.apiService.sshNodePools }}
        - name: skypilot-ssh-node-pools
          mountPath: /var/skypilot/ssh_node_pool
        {{- end }}
        {{- if .Values.apiService.sshKeySecret }}
        - name: skypilot-ssh-identity
          mountPath: /var/skypilot/ssh_keys
        {{- end }}
        {{- if .Values.slurmCredentials.config }}
        - name: slurm-config
          mountPath: /root/.slurm
        {{- end }}
        {{- if .Values.awsCredentials.enabled }}
        - name: {{ ternary "aws-config-files" "aws-config" .Values.awsCredentials.useCredentialsFile }}
          mountPath: /root/.aws
          readOnly: true
        {{- end }}
        {{- if .Values.r2Credentials.enabled }}
        - name: r2-config
          mountPath: /root/.cloudflare
          readOnly: true
        {{- end }}
        {{- if .Values.coreweaveCredentials.enabled }}
        - name: coreweave-config
          mountPath: /root/.coreweave
          readOnly: true
        {{- end }}
        {{- if .Values.gcpCredentials.enabled }}
        - name: gcp-config
          mountPath: /root/.config/gcloud
        - name: gcp-credentials
          mountPath: /root/gcp-cred.json
          subPath: gcp-cred.json
        {{- end }}
        {{- if .Values.kubernetesCredentials.useKubeconfig }}
        - name: kube-config
          mountPath: {{ ternary "/var/skypilot/kubeconfig" "/root/.kube" (not (empty .Values.apiService.sshNodePools)) }}
        {{- end }}
        {{- if .Values.apiService.sshNodePools }}
        - name: state-volume
          mountPath: /root/.kube
          subPath: .kube
        {{- end }}
        {{- if .Values.runpodCredentials.enabled }}
        - name: runpod-config
          mountPath: /root/.runpod
          readOnly: true
        {{- end }}
        {{- if .Values.digitaloceanCredentials.enabled }}
        - name: digitalocean-config
          mountPath: /root/.config/doctl
          readOnly: true
        {{- end }}
        {{- if .Values.lambdaCredentials.enabled }}
        - name: lambda-config
          mountPath: /root/.lambda_cloud
          readOnly: true
        {{- end }}
        {{- if .Values.vastCredentials.enabled }}
        - name: vast-config
          mountPath: /root/.config/vastai
          readOnly: true
        {{- end }}
        {{- if .Values.nebiusCredentials.enabled }}
        - name: nebius-credentials
          mountPath: /root/.nebius_credentials
        {{- end }}
        {{- if $externalLBEnabled }}
        - name: skypilot-serve-lb-sync-auth
          mountPath: /etc/skypilot/serve-auth/lb-sync
          readOnly: true
        - name: skypilot-serve-controller-admin-auth
          mountPath: /etc/skypilot/serve-auth/controller-admin
          readOnly: true
        {{- if $lbDataPlaneAuthEnabled }}
        - name: skypilot-serve-lb-auth
          mountPath: /etc/skypilot/serve-auth/lb-data-plane
          readOnly: true
        {{- end }}
        {{- end }}
        {{- if $authorityPreflightEnabled }}
        - name: skypilot-resource-action-authority-manifest
          mountPath: /etc/skypilot/resource-action-authority/manifest.json
          subPath: manifest.json
          readOnly: true
        - name: skypilot-resource-action-authority-auth
          mountPath: /etc/skypilot/resource-action-authority/auth
          readOnly: true
        - name: skypilot-resource-action-authority-ca
          mountPath: /etc/skypilot/resource-action-authority/tls
          readOnly: true
        {{- end }}
        {{- with .Values.databaseConnection.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.apiService.extraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with $controllerExtraVolumeMounts }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      initContainers:
      {{- if and .Values.awsCredentials.enabled (not .Values.awsCredentials.useCredentialsFile) }}
      - name: create-aws-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          set -e
          mkdir -p /root/.aws
          aws configure set aws_access_key_id "$AWS_ACCESS_KEY_ID"
          aws configure set aws_secret_access_key "$AWS_SECRET_ACCESS_KEY"
        env:
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: {{ .Values.awsCredentials.awsSecretName }}
              key: {{ .Values.awsCredentials.accessKeyIdKeyName }}
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.awsCredentials.awsSecretName }}
              key: {{ .Values.awsCredentials.secretAccessKeyKeyName }}
        volumeMounts:
        - name: aws-config
          mountPath: /root/.aws
      {{- end }}
      {{- if .Values.r2Credentials.enabled }}
      - name: setup-r2-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          set -e
          mkdir -p /root/.cloudflare
          printf '%s\n' "$R2_CREDENTIALS" > /root/.cloudflare/r2.credentials
          printf '%s\n' "$R2_ACCOUNT_ID" > /root/.cloudflare/accountid
          chmod 600 /root/.cloudflare/r2.credentials /root/.cloudflare/accountid
        env:
        - name: R2_CREDENTIALS
          valueFrom:
            secretKeyRef:
              name: {{ .Values.r2Credentials.r2SecretName }}
              key: r2.credentials
        - name: R2_ACCOUNT_ID
          valueFrom:
            secretKeyRef:
              name: {{ .Values.r2Credentials.r2SecretName }}
              key: accountid
        volumeMounts:
        - name: r2-config
          mountPath: /root/.cloudflare
      {{- end }}
      {{- if .Values.gcpCredentials.enabled }}
      - name: setup-gcp-credentials
        image: {{ include "common.image" (dict "root" . "image" .Values.gcpCredentials.image) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          gcloud auth activate-service-account --key-file=/root/gcp-cred.json
          gcloud config set project {{ .Values.gcpCredentials.projectId }}
        volumeMounts:
        - name: gcp-credentials
          mountPath: /root/gcp-cred.json
          subPath: gcp-cred.json
        - name: gcp-config
          mountPath: /root/.config/gcloud
      {{- end }}
      {{- if .Values.coreweaveCredentials.enabled }}
      - name: setup-coreweave-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - cp /root/.coreweave_credentials/* /root/.coreweave/
        volumeMounts:
        - name: coreweave-credentials
          mountPath: /root/.coreweave_credentials
        - name: coreweave-config
          mountPath: /root/.coreweave
      {{- end }}
      {{- if .Values.runpodCredentials.enabled }}
      - name: create-runpod-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          set -e
          mkdir -p /root/.runpod
          printf '[default]\napi_key = "%s"\n' "$RUNPOD_API_KEY" > /root/.runpod/config.toml
        env:
        - name: RUNPOD_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.runpodCredentials.runpodSecretName }}
              key: api_key
        volumeMounts:
        - name: runpod-config
          mountPath: /root/.runpod
      {{- end }}
      {{- if .Values.digitaloceanCredentials.enabled }}
      - name: create-digitalocean-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          set -e
          mkdir -p /root/.config/doctl
          printf 'access-token: %s\n' "$DIGITALOCEAN_CREDENTIALS" > /root/.config/doctl/config.yaml
        env:
        - name: DIGITALOCEAN_CREDENTIALS
          valueFrom:
            secretKeyRef:
              name: {{ .Values.digitaloceanCredentials.digitaloceanSecretName }}
              key: api_key
        volumeMounts:
        - name: digitalocean-config
          mountPath: /root/.config/doctl
      {{- end }}
      {{- if .Values.lambdaCredentials.enabled }}
      - name: create-lambda-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          set -e
          mkdir -p /root/.lambda_cloud
          printf 'api_key = %s\n' "$LAMBDA_API_KEY" > /root/.lambda_cloud/lambda_keys
        env:
        - name: LAMBDA_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.lambdaCredentials.lambdaSecretName }}
              key: api_key
        volumeMounts:
        - name: lambda-config
          mountPath: /root/.lambda_cloud
      {{- end }}
      {{- if .Values.vastCredentials.enabled }}
      - name: create-vast-credentials
        image: {{ include "common.image" (dict "root" . "image" $controllerImage) }}
        command: ["/bin/sh", "-c"]
        args:
        - |
          set -e
          mkdir -p /root/.config/vastai
          printf '%s\n' "$VAST_API_KEY" > /root/.config/vastai/vast_api_key
        env:
        - name: VAST_API_KEY
          valueFrom:
            secretKeyRef:
              name: {{ .Values.vastCredentials.vastSecretName }}
              key: api_key
        volumeMounts:
        - name: vast-config
          mountPath: /root/.config/vastai
      {{- end }}
      {{- with .Values.extraInitContainers }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      volumes:
      - name: skypilot-role-runtime
        emptyDir: {}
      - name: state-volume
        persistentVolumeClaim:
          claimName: {{ $fullName }}-state
      - name: skypilot-config
        configMap:
          name: {{ $fullName }}-config
      {{- if or .Values.auth.externalProxy.enabled (eq (include "skypilot.ingressOAuthEnabled" .) "true") }}
      - name: skypilot-server-config
        configMap:
          name: {{ $fullName }}-server-config
      {{- end }}
      {{- if .Values.apiService.sshNodePools }}
      - name: skypilot-ssh-node-pools
        secret:
          secretName: {{ $fullName }}-ssh-node-pools
      {{- end }}
      {{- if .Values.apiService.sshKeySecret }}
      - name: skypilot-ssh-identity
        secret:
          secretName: {{ .Values.apiService.sshKeySecret }}
          defaultMode: 0600
      {{- end }}
      {{- if .Values.slurmCredentials.config }}
      - name: slurm-config
        secret:
          secretName: {{ $fullName }}-slurm-config
      {{- end }}
      {{- if .Values.awsCredentials.enabled }}
      - name: {{ ternary "aws-config-files" "aws-config" .Values.awsCredentials.useCredentialsFile }}
        {{- if .Values.awsCredentials.useCredentialsFile }}
        secret:
          secretName: {{ .Values.awsCredentials.awsSecretName }}
        {{- else }}
        emptyDir: {}
        {{- end }}
      {{- end }}
      {{- if .Values.gcpCredentials.enabled }}
      - name: gcp-credentials
        secret:
          secretName: {{ .Values.gcpCredentials.gcpSecretName }}
      - name: gcp-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.coreweaveCredentials.enabled }}
      - name: coreweave-credentials
        secret:
          secretName: {{ .Values.coreweaveCredentials.coreweaveSecretName }}
      - name: coreweave-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.runpodCredentials.enabled }}
      - name: runpod-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.digitaloceanCredentials.enabled }}
      - name: digitalocean-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.lambdaCredentials.enabled }}
      - name: lambda-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.vastCredentials.enabled }}
      - name: vast-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.r2Credentials.enabled }}
      - name: r2-config
        emptyDir: {}
      {{- end }}
      {{- if .Values.nebiusCredentials.enabled }}
      - name: nebius-credentials
        secret:
          secretName: {{ .Values.nebiusCredentials.nebiusSecretName }}
      {{- end }}
      {{- if .Values.kubernetesCredentials.useKubeconfig }}
      - name: kube-config
        secret:
          secretName: {{ .Values.kubernetesCredentials.kubeconfigSecretName }}
      {{- end }}
      {{- if $externalLBEnabled }}
      - name: skypilot-serve-lb-sync-auth
        projected:
          defaultMode: 0400
          sources:
          - secret:
              name: {{ $lbSyncSecret | quote }}
              items:
              - key: {{ $lbSyncKey | quote }}
                path: tokens
      - name: skypilot-serve-controller-admin-auth
        projected:
          defaultMode: 0400
          sources:
          - secret:
              name: {{ $controllerAdminSecret | quote }}
              items:
              - key: {{ $controllerAdminKey | quote }}
                path: tokens
      {{- if $lbDataPlaneAuthEnabled }}
      - name: skypilot-serve-lb-auth
        projected:
          defaultMode: 0400
          sources:
          - secret:
              name: {{ $lbDataPlaneSecret | quote }}
              items:
              - key: {{ $lbDataPlaneKey | quote }}
                path: tokens
      {{- end }}
      {{- end }}
      {{- if $authorityPreflightEnabled }}
      - name: skypilot-resource-action-authority-manifest
        configMap:
          name: {{ $authorityManifestConfigMap | quote }}
          defaultMode: 0444
          items:
          - key: manifest.json
            path: manifest.json
      - name: skypilot-resource-action-authority-auth
        secret:
          secretName: {{ $authorityAuthSecret | quote }}
          defaultMode: 0400
          items:
          - key: {{ $authorityAuthKey | quote }}
            path: tokens
      - name: skypilot-resource-action-authority-ca
        secret:
          secretName: {{ $authorityTLSSecret | quote }}
          defaultMode: 0400
          items:
          - key: {{ $authorityTLSCAKey | quote }}
            path: ca.crt
      {{- end }}
      {{- with .Values.databaseConnection.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with .Values.apiService.extraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with $controllerExtraVolumes }}
      {{- toYaml . | nindent 6 }}
      {{- end }}
      {{- with $controllerNodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $controllerTolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $controllerAffinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if $controllerTopologySpreadTopologyKey }}
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: {{ $controllerTopologySpreadTopologyKey }}
        whenUnsatisfiable: DoNotSchedule
        labelSelector:
          matchLabels:
            app: {{ $fullName }}-controller
      {{- end }}
{{- end }}
