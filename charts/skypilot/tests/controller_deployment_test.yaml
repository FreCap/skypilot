# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: controller_deployment_test
templates:
  - templates/controller-deployment.yaml
tests:
  - it: should not render an controller deployment in compatibility mode
    asserts:
      - hasDocuments:
          count: 0

  - it: should render active-standby fenced controllers in HA mode
    set:
      apiService.highAvailability.enabled: true
      apiService.replicas: 2
      apiService.upgradeStrategy: RollingUpdate
      apiService.dbConnectionSecretName: external-db
      requestStore.backend: postgres
      storage.enabled: true
      storage.accessMode: ReadWriteMany
      controllerService.replicas: 2
      executorService.terminationGracePeriodSeconds: 75
    asserts:
      - equal:
          path: metadata.name
          value: RELEASE-NAME-controller
      - equal:
          path: spec.replicas
          value: 2
      - equal:
          path: spec.strategy.type
          value: RollingUpdate
      - equal:
          path: spec.strategy.rollingUpdate.maxUnavailable
          value: 0
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_API_SERVER_ROLE
            value: controller
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_API_SERVER_ENDPOINT
            value: http://RELEASE-NAME-api-service.NAMESPACE.svc
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_CONTROLLER_CUTOVER_QUIESCENCE_SECONDS
            value: "85"
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_API_SERVER_INSTANCE_ID
            valueFrom:
              fieldRef:
                fieldPath: metadata.uid
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_DB_CONNECTION_URI
            valueFrom:
              secretKeyRef:
                name: external-db
                key: connection_string
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.httpGet.path
          value: /readyz
      - equal:
          path: spec.template.spec.containers[0].livenessProbe.httpGet.path
          value: /livez
      - equal:
          path: spec.template.spec.containers[0].lifecycle.preStop.exec.command
          value:
            - /bin/sh
            - -c
            - touch /var/run/skypilot/draining && sleep 20
      - equal:
          path: spec.template.spec.topologySpreadConstraints[0].topologyKey
          value: kubernetes.io/hostname
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: skypilot-role-runtime
            mountPath: /var/run/skypilot
      - contains:
          path: spec.template.spec.volumes
          content:
            name: skypilot-role-runtime
            emptyDir: {}
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: state-volume
            mountPath: /root/.sky
            subPath: .sky

  - it: should allow an explicit controller topology-spread opt-out
    set:
      apiService.highAvailability.enabled: true
      controllerService.topologySpreadTopologyKey: ""
    asserts:
      - notExists:
          path: spec.template.spec.topologySpreadConstraints

  - it: should leave authority purpose names available while disabled
    values:
      - fixtures/resource-action-authority-values.yaml
    set:
      resourceActions.authorityWorker.enabled: false
      controllerService.extraEnvs:
        - name: SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE
          value: /compat/tokens
      controllerService.extraVolumes:
        - name: skypilot-resource-action-authority-auth
          emptyDir: {}
        - name: skypilot-resource-action-authority-ca
          emptyDir: {}
        - name: skypilot-resource-action-authority-manifest
          emptyDir: {}
      controllerService.extraVolumeMounts:
        - name: skypilot-resource-action-authority-auth
          mountPath: /etc/skypilot/resource-action-authority/auth
        - name: skypilot-resource-action-authority-ca
          mountPath: /etc/skypilot/resource-action-authority/tls
        - name: skypilot-resource-action-authority-manifest
          mountPath: /etc/skypilot/resource-action-authority/manifest.json
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE
            value: /compat/tokens
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED
      - contains:
          path: spec.template.spec.volumes
          content:
            name: skypilot-resource-action-authority-manifest
            emptyDir: {}
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: skypilot-resource-action-authority-manifest
            mountPath: /etc/skypilot/resource-action-authority/manifest.json

  - it: should emit the activation marker with live preflight wiring
    values:
      - fixtures/resource-action-authority-values.yaml
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED
            value: "true"
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE
            value: /etc/skypilot/resource-action-authority/auth/tokens

  - it: should omit controller activation for a tombstone-only release
    values:
      - fixtures/resource-action-authority-values.yaml
      - fixtures/resource-action-authority-tombstone-values.yaml
    asserts:
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED
      - notContains:
          path: spec.template.spec.containers[0].env
          content:
            name: SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE

  - it: should reserve the authority purpose env while enabled
    values:
      - fixtures/resource-action-authority-values.yaml
    set:
      controllerService.extraEnvs:
        - name: SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE
          value: /compat/tokens
    asserts:
      - failedTemplate:
          errorMessage: environment variable "SKYPILOT_RESOURCE_ACTION_PREFLIGHT_AUTH_TOKENS_FILE" is duplicated or reserved by the chart

  - it: should reserve the chart-owned activation marker
    values:
      - fixtures/resource-action-authority-values.yaml
    set:
      controllerService.extraEnvs:
        - name: SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED
          value: "true"
    asserts:
      - failedTemplate:
          errorMessage: environment variable "SKYPILOT_RESOURCE_ACTION_AUTHORITY_ENABLED" is duplicated or reserved by the chart

  - it: should reserve authority purpose volume names while enabled
    values:
      - fixtures/resource-action-authority-values.yaml
    set:
      controllerService.extraVolumes:
        - name: skypilot-resource-action-authority-manifest
          emptyDir: {}
    asserts:
      - failedTemplate:
          errorMessage: volume name "skypilot-resource-action-authority-manifest" is duplicated or reserved by the chart

  - it: should reserve authority purpose mount paths while enabled
    values:
      - fixtures/resource-action-authority-values.yaml
    set:
      controllerService.extraVolumeMounts:
        - name: compatibility-manifest
          mountPath: /etc/skypilot/resource-action-authority/manifest.json
    asserts:
      - failedTemplate:
          errorMessage: volume mount path "/etc/skypilot/resource-action-authority/manifest.json" is duplicated or reserved by the chart

  - it: should reject a custom controller API endpoint
    set:
      apiService.highAvailability.enabled: true
      controllerService.extraEnvs:
        - name: SKYPILOT_API_SERVER_ENDPOINT
          value: http://client-controlled-endpoint
    asserts:
      - failedTemplate:
          errorMessage: 'environment variable "SKYPILOT_API_SERVER_ENDPOINT" is duplicated or reserved by the chart'

  - it: should share database and explicit controller pod extensions
    set:
      apiService.highAvailability.enabled: true
      apiService.replicas: 2
      apiService.upgradeStrategy: RollingUpdate
      apiService.dbConnectionSecretName: external-db
      requestStore.backend: postgres
      storage.enabled: true
      storage.accessMode: ReadWriteMany
      databaseConnection.extraEnvs:
        - name: PGSSLMODE
          value: verify-full
      controllerService.extraEnvs:
        - name: EXECUTOR_SETTING
          value: enabled
      controllerService.extraVolumes:
        - name: controller-secret
          secret:
            secretName: controller-secret
      controllerService.extraVolumeMounts:
        - name: controller-secret
          mountPath: /etc/controller
          readOnly: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: PGSSLMODE
            value: verify-full
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: EXECUTOR_SETTING
            value: enabled
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: controller-secret
            mountPath: /etc/controller
            readOnly: true
      - contains:
          path: spec.template.spec.volumes
          content:
            name: controller-secret
            secret:
              secretName: controller-secret

  - it: should project native cloud credentials into controllers
    set:
      apiService.highAvailability.enabled: true
      apiService.replicas: 2
      apiService.upgradeStrategy: RollingUpdate
      apiService.dbConnectionSecretName: external-db
      requestStore.backend: postgres
      storage.enabled: true
      storage.accessMode: ReadWriteMany
      awsCredentials.enabled: true
      awsCredentials.useCredentialsFile: true
      awsCredentials.awsSecretName: aws-credentials
      gcpCredentials.enabled: true
      gcpCredentials.gcpSecretName: gcp-credentials
      gcpCredentials.projectId: test-project
    asserts:
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: aws-config-files
            mountPath: /root/.aws
            readOnly: true
      - contains:
          path: spec.template.spec.containers[0].volumeMounts
          content:
            name: gcp-credentials
            mountPath: /root/gcp-cred.json
            subPath: gcp-cred.json
      - contains:
          path: spec.template.spec.volumes
          content:
            name: aws-config-files
            secret:
              secretName: aws-credentials
      - contains:
          path: spec.template.spec.volumes
          content:
            name: gcp-credentials
            secret:
              secretName: gcp-credentials

  - it: should allow custom controller GCP credentials when native credentials are disabled
    set:
      apiService.highAvailability.enabled: true
      gcpCredentials.enabled: false
      controllerService.extraEnvs:
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: /var/secrets/gcp/custom.json
      controllerService.extraVolumes:
        - name: gcp-credentials
          secret:
            secretName: custom-gcp-credentials
      controllerService.extraVolumeMounts:
        - name: gcp-credentials
          mountPath: /var/secrets/gcp
          readOnly: true
    asserts:
      - contains:
          path: spec.template.spec.containers[0].env
          content:
            name: GOOGLE_APPLICATION_CREDENTIALS
            value: /var/secrets/gcp/custom.json
      - contains:
          path: spec.template.spec.volumes
          content:
            name: gcp-credentials
            secret:
              secretName: custom-gcp-credentials

  - it: should reject custom controller GCP credentials when native credentials are enabled
    set:
      apiService.highAvailability.enabled: true
      gcpCredentials.enabled: true
      controllerService.extraEnvs:
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: /var/secrets/gcp/custom.json
    asserts:
      - failedTemplate:
          errorMessage: 'environment variable "GOOGLE_APPLICATION_CREDENTIALS" is duplicated or reserved by the chart'
